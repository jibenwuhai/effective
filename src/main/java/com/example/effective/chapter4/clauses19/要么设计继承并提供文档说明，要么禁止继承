要么设计继承并提供文档说明，要么禁止继承
该类必须有文档说明它可覆盖（overridable）的方法的自用性（self-use）
类必须以精心挑选的受保护（protected）方法的形式，提供适当的钩子（hook），以便进入其内部工作中。
对于未了继承而设计的类，唯一的测试方法就是编写子类。
必须在发布类之前先编写子类进行测试。
构造器决不能调用可被覆盖的方法
无论是clone还是readObject，都不可以调用可覆盖的方法，不管是以直接还是间接的方式。
为继承而设计的类，对这个类会有一些实质性的限制。
对于那些并非为了安全地进行子类化而设计和编写文档的类，要禁止子类化。
